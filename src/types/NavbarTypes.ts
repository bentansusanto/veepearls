export type NavProps = {
    pages: string;
    link: string;
    subMenu: SubMenu[] 
}

export type SubMenu = {
    name_menu: string;
    link: string;
}

export type SocialMedia = {
    icon: string;
    link: string;
}